struct CustomViewModifier: ViewModifier {
    
    func body(content: Content) -> some View {
        content
        
    }
}

extension View {
    func witchCustomViewModifier() -> some View {
        modifier(CustomViewModifier())
    }
}
